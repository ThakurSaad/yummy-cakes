import React from 'react';

const Cart = ({cart}) => {
    console.log(cart);
    return (
        <div className='cart'>
            <h3>Selected Cakes</h3>
        </div>
    );
};

export default Cart;